# Unused GitHub Pages template

This directory is an inherited Jekyll template. It is **not** what deploys.

The live site at https://vindeckyy.github.io/Solar-Flare/ is built from
`website/` by `.github/workflows/update-pages.yml`.

Edit marketing copy under `website/components/solarflare/` instead of the
files here.
